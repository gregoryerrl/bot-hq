# Binary
/bot-hq

# Database
data/
bot-hq.db
*.db

# env files
.env*
!.env.example

# Build output
/out/
/dist/
/target/

# Worktrees
.worktrees/

# Misc
.DS_Store
*.pem

# Local Claude Code harness state (per-repo settings, locks) — not for commit
.claude/

# Stale coder configs
.bot-hq-coder-*-mcp.json

# Transient debug + audit artifacts (not for commit)
*.log
/Screenshot*.png
/phase-*-audit.csv
/docs/commit-message-draft.md

# Test-run scratch (browser snapshots, screenshots, etc.)
/tmp/

# Frontend
node_modules/
/frontend/node_modules/
/frontend/dist/
/frontend/.vite/
/.vite/
/frontend/tsconfig.tsbuildinfo

# Out-of-tree cargo target dir used when building in a container (the Fedora
# smoke test builds with CARGO_TARGET_DIR=target-container so the host and
# container trees do not fight). Multi-GB — the `/target/` anchor above does
# not cover it.
/target-container/
/frontend/**/tsconfig.tsbuildinfo

# Tauri
/gen/
/capabilities/gen/
# leftover of the deleted bench/ Python harnesses (2026-08-17)
.pytest_cache/
