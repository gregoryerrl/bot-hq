{
  "name": "github",
  "version": "1.0.0",
  "description": "GitHub integration for Bot-HQ - sync issues, create PRs, and more",
  "author": {
    "name": "Bot-HQ Team"
  },
  "license": "MIT",

  "bot-hq": {
    "minVersion": "1.0.0"
  },

  "mcp": {
    "entry": "./dist/server.js",
    "transport": "stdio",
    "tools": [
      "github_sync_issues",
      "github_create_pr",
      "github_push_branch",
      "github_get_issue"
    ]
  },

  "extensions": "./dist/extensions.js",

  "settings": {
    "autoSync": {
      "type": "boolean",
      "label": "Auto-sync issues",
      "description": "Automatically sync GitHub issues on startup",
      "default": false
    },
    "syncInterval": {
      "type": "number",
      "label": "Sync interval (minutes)",
      "description": "How often to check for new issues (0 = manual only)",
      "default": 0
    }
  },

  "credentials": {
    "GITHUB_TOKEN": {
      "type": "secret",
      "label": "GitHub Personal Access Token",
      "description": "Token with repo and issue permissions. Get one at github.com/settings/tokens",
      "required": true
    }
  },

  "permissions": [
    "workspace:read",
    "workspace:write",
    "task:read",
    "task:write",
    "task:create",
    "approval:read"
  ]
}
