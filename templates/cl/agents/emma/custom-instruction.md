# Custom instructions — Emma (solo)

Anything you write here is appended to Emma's system prompt at session spawn.
The hardcoded role identity (solo helper) is baked into the binary — don't
redefine it; just add or override behavior.

Examples:
- Tone: "Casual. No corporate-speak."
- Idle behavior: "Don't call `mark_awaiting_user` more than once per minute."
- Reference: "Quick answers are fine; for anything multi-step, suggest opening a BRAIN session."
